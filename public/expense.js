let mealsButtonNode = document.querySelector('#mealButton');

mealsButtonNode.addEventListener('click', event => {
  console.log('Clicked on Meals Button');
  // let inputNodes = document.querySelectorAll("input[name='price[]']");
  // let sum = 0;

  // inputNodes.forEach(node => sum += Number.parseFloat(node.value));

  // document.querySelector('#results').textContent = sum.toFixed(2);
  // ('#expenses')
});
